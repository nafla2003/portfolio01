# Seidh Portfolio — Clone

This repository is a recreated static clone (React + TypeScript + Vite) of https://seidh-portfolio.netlify.app/ for learning and local use.

## Setup

Install dependencies:

```bash
npm install
```

Run dev server:

```bash
npm run dev
```

Build for production:

```bash
npm run build
```

Preview production build:

```bash
npm run preview
```

Notes:
- The profile photo is referenced from the original site's public URL.
- This project reproduces structure and styles to closely match the original portfolio.
